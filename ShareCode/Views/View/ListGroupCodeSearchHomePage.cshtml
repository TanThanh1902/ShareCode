@using ShareCode.Models;
@model List<tblGroupCode>

@foreach (var item in Model)
{
    <a class="dropdown-item " href="@Url.Action("FilterPostsByGroupCodeID", "Search", new { id = item.Group_ID})">@item.Group_Name</a>
}
